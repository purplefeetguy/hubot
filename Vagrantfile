# -*- mode: ruby -*-
# vi: set ft=ruby :

# Vagrantfile API/syntance version.
VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|
  config.vm.box = "ubuntu/trustry64"
  config.vm.hostname = "hubot-dev"
end
